This is a demo for ansible starting up a docker container with Apache2 
installed.

To start the container:

  vagrant up

To stop the container:

  vagrant destroy

To open an SSH connection to the container:

  vagrant ssh


- HTTP port is 80 and SSH port is 2222
